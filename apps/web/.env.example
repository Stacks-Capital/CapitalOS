# Copy to .env.local. Only publishable values belong here: this file ends up in the browser bundle.
VITE_API_BASE_URL=http://127.0.0.1:3000
VITE_CLIENT_ID=pk_fixture_sandbox
# Starting network. The app can switch to testnet without rebuilding; testnet writes stay disabled.
VITE_NETWORK=mainnet
