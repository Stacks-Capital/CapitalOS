# Used only by the browser tests (vite --mode e2e). Vite ranks a mode file above .env.local,
# so the tests reach the test API whatever a developer has locally. Publishable values only.
VITE_API_BASE_URL=http://127.0.0.1:3100
VITE_CLIENT_ID=pk_fixture_sandbox
VITE_NETWORK=mainnet
