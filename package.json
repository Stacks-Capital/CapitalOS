{
  "name": "capitalos",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.4.1",
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/config && tsc --noEmit -p packages/wallets && tsc --noEmit -p packages/adapters && tsc --noEmit -p packages/fixtures && tsc --noEmit -p scripts/contracts && tsc --noEmit -p scripts/probes",
    "test:unit": "node --test --experimental-strip-types packages/core/src/*.test.ts packages/config/src/*.test.ts packages/wallets/src/*.test.ts packages/adapters/src/*.test.ts",
    "test:e2e": "node --test --experimental-strip-types packages/fixtures/src/*.test.ts",
    "test": "pnpm typecheck && pnpm test:unit && pnpm test:e2e",
    "probe:contracts": "node --experimental-strip-types scripts/contracts/probe.ts"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.2",
    "@types/node": "^24.13.4",
    "typescript": "6.0.3"
  }
}
